import Layout from '../../components/Layout';
import Calendar from '../../components/calendar';

const CalendarPage = () => {
  return (
    <Layout title="Calendar" hasTitle={false}>
      <Calendar />
    </Layout>
  );
};
export default CalendarPage;
